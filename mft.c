#include<stdio.h>
int main()
{
	int i,n,ms,s,ps[10],p[10],intr=0,size;
	printf("enter size of memory");
	scanf("%d",&ms);
	printf("enter memory for os");
	scanf("%d",&s);
	printf("enter no.of partitions to be divided");
	scanf("%d",&n);
	ms=ms-s;
	for(i=0;i<n;i++)
	{
		printf("enter process and process size");
		scanf("%d %d",&ps[i]);
	if(ps[1]<=size)
	{
		printf("process is allocated");
	}
	else
	{
		printf("process is blocked");
		printf("/n total fragmentation is:");
	}
	getch();
}
}









	
